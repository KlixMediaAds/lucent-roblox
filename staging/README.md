Files here are NOT synced by Rojo — a holding area for assets that need rework.
Pinetree.rbxm: Rojo cannot parse it (CylinderMesh.Tags serialization). Re-export from
Studio as .rbxmx into assets/ to replace it.
